# DANAConnect Framework Tests — Playwright Dependencies
# Install with: pip install -r requirements.txt
# Then install browsers: playwright install

pytest==9.0.3                  # Test runner
playwright==1.58.0             # Browser automation (Python 3.13 compatible)
pytest-playwright==0.7.2       # Playwright + pytest integration
python-dotenv==1.2.2           # Load .env credentials
allure-pytest==2.15.3          # Allure reporting integration
