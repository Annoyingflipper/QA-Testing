# DANAConnect Framework Tests — Selenium Dependencies
# Install with: pip install -r requirements.txt
# ChromeDriver is auto-downloaded at runtime by webdriver-manager — no extra step.

pytest==9.0.3                  # Test runner (matches Playwright)
selenium==4.19.0               # Browser automation (ships Selenium Manager — auto-downloads ChromeDriver)
python-dotenv==1.2.2           # Load .env credentials (matches Playwright)
allure-pytest==2.15.3          # Allure reporting integration (matches Playwright)
