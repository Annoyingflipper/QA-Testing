# ============================================================
# .ENV.EXAMPLE — Template for environment variables
# ============================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# The .env file is in .gitignore — it NEVER gets committed.
# This .env.example is safe to commit because it has no secrets.
# ============================================================

BASE_URL=https://portal.danaconnect.com
TEST_COMPANY_CODE=your_company_code_here
TEST_USERNAME=your_username_here
TEST_PASSWORD=your_password_here
